# Snapshot

Snapshot is the fast-view layer.

Use this for:
- session handoffs
- project status views
- startup context for AI sessions
- HTML-facing or dashboard-facing "current state" outputs

Current subfolders:
- `session-handoffs`
- `project-status`
- `html-view`
